### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Category {
  id: Int!
  name: String!
}

type Item {
  category: Category
  id: Int!
  image: String
  name: String!
  note: String
}

type List {
  id: Int!
  items: [ListItem!]!
  name: String!
}

type ListItem {
  category: Category
  count: Int!
  itemId: Int!
  listId: Int!
  name: String!
}

type Mutation {
  createCategory(name: String!): Category
  createItem(categoryId: Int!, image: Upload, name: String!, note: String): Item
  createList(items: [Int!], name: String!): List
  deleteCategory(id: Int!): Category
  deleteItem(itemId: Int!): Item
  deleteList(id: Int!): List
  signin(password: String!, username: String!): User
  signout: Boolean
  signup(password: String!, username: String!): User
  updateCategory(id: Int!, name: String!): Category
  updateItem(categoryId: Int, image: Upload, itemId: Int!, name: String, note: String): Item
  updateList(add: [Int!], delete: [Int!], id: Int!, name: String): List
}

type Query {
  categories: [Category!]!
  items: [Item!]!
  lists: [List!]!
  me: User
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload

type UploadedFile {
  filename: String!
  uri: String!
}

type User {
  activeListId: Int
  categories: [Category!]!
  id: Int!
  items: [Item!]
  lists: [List!]
  username: String!
}
