### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Category {
  id: Int!
  name: String!
}

type Item {
  category: Category
  id: Int!
  image: String
  name: String!
  note: String
}

type List {
  id: Int!
  items: [ListItem!]!
  name: String!
}

type ListItem {
  category: Category
  complete: Boolean!
  count: Int!
  itemId: Int!
  listId: Int!
  name: String!
}

type Mutation {
  cancelList(id: Int!): List
  completeList(id: Int!): List
  createCategory(name: String!): Category
  createItem(categoryId: Int!, image: String, name: String!, note: String): Item
  createList(items: [Int!], name: String!): List
  decrementListItemCount(itemId: Int!, listId: Int!): ListItem
  deleteCategory(id: Int!): Category
  deleteItem(id: Int!): Item
  incrementListItemCount(itemId: Int!, listId: Int!): ListItem
  setListItemComplete(complete: Boolean!, itemId: Int!, listId: Int!): ListItem
  signin(password: String!, username: String!): User
  signout: Boolean
  signup(password: String!, username: String!): User
  updateCategory(id: Int!, name: String!): Category
  updateItem(categoryId: Int, image: String, itemId: Int!, name: String, note: String): Item
  updateList(add: [Int!], delete: [Int!], id: Int, name: String): List
}

type Query {
  categories: [Category!]!
  item(id: Int!): Item
  items: [Item!]!
  lists: [List!]!
  me: User
}

type User {
  activeListId: Int
  categories: [Category!]!
  id: Int!
  items: [Item!]
  lists: [List!]
  username: String!
}
