### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Category {
  id: Int!
  name: String!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

type Item {
  category: Category
  id: Int!
  image: String
  name: String!
  note: String
}

type List {
  createdAt: DateTime!
  id: Int!
  items: [ListItem!]!
  name: String!
  status: ListStatus!
  updatedAt: DateTime!
}

type ListItem {
  category: Category
  complete: Boolean!
  count: Int!
  itemId: Int!
  listId: Int!
  name: String!
}

enum ListStatus {
  ACTIVE
  CANCELLED
  COMPLETED
}

type Mutation {
  addItem(itemId: Int!, listId: Int): List
  cancelList(id: Int!): List
  completeList(id: Int!): List
  createCategory(name: String!): Category
  createItem(categoryId: Int!, image: String, name: String!, note: String): Item
  decrementListItemCount(itemId: Int!, listId: Int!): ListItem
  deleteCategory(id: Int!): Category
  deleteItem(id: Int!): Item
  incrementListItemCount(itemId: Int!, listId: Int!): ListItem
  removeItem(itemId: Int!, listId: Int!): List
  renameList(listId: Int!, name: String!): List
  setListItemComplete(complete: Boolean!, itemId: Int!, listId: Int!): ListItem
  signin(password: String!, username: String!): User
  signout: Boolean
  signup(password: String!, username: String!): User
}

type Query {
  categories: [Category!]!
  item(id: Int!): Item
  items: [Item!]!
  lists: [List!]!
  me: User
}

type User {
  activeListId: Int
  categories: [Category!]!
  id: Int!
  items: [Item!]
  lists: [List!]
  username: String!
}
